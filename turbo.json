{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "outputs": [".next/**", "!.next/cache/**", "lib/**"] },
    "dev": { "cache": false, "persistent": true },
    "lint": {}
  }
}
